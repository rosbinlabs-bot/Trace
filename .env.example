VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only (Vercel project env, NOT prefixed VITE_ so it never reaches the browser bundle).
# Powers Phase Management's AI Insights panel (api/insights.ts).
ANTHROPIC_API_KEY=
# Optional, defaults to claude-sonnet-5 if unset.
ANTHROPIC_MODEL=
